# PRD v3: 웹 개념 이해 점검 도구 (Mindi)

> ## v2 → v3 변경 요약
>
> v2는 제품 기획으로는 완결되어 있으나, **본 과제(근거 기반 RAG 안내 챗봇)의 산출물로는 종류가 달랐다.**
> v3는 제품의 핵심(루브릭 채점)을 그대로 두고, 과제가 요구하는 것을 제자리에 넣는다.
>
> 1. **검색의 역할을 명시적으로 선언했다 (7장 신규).** 루브릭 채점이 핵심이라면 "그렇다면 검색은 왜 있는가"에 답해야 한다. 판정에는 검색을 쓰지 않는다고 먼저 선을 긋고, 검색이 실제로 일하는 세 자리와 **그중 MVP 규모에서 정당화되는 것이 무엇인지**를 정직하게 적었다.
> 2. **거절(refusal) 경로를 제품에 넣었다 (F8, 9장).** v2에는 사용자가 범위 밖을 묻거나 정답을 요구할 자리가 흐름도에 없었다. 실제로는 가장 자주 일어나는 분기다.
> 3. **판정이 두 층임을 드러냈다 (12장 신규).** Mindi의 판정(사용자 설명 채점)과 과제의 판정(챗봇 진단의 근거성 채점)은 대상이 다르다. 매핑표를 넣었다.
> 4. **개념 6개 → 2개.** A2(HTTP) + B1(박스 모델). Group A/B 비교의 최소 표본은 유지한다.
> 5. **예시 루브릭의 오류를 수정했다.** `프로토콜 — 규칙·약속` 필수 요소는 **근거 원문에 없는 기준**이었다. 원문에 실제로 있는 "요청이 클라이언트 쪽에서 시작된다"로 교체했다. (6.3 검증 기록)
> 6. **A3(웹페이지 표시 과정)을 제외했다.** 근거로 지정한 MDN 문서가 **존재하지 않는다**(404). 실제 내용은 3개 문서에 흩어져 있어 가장 비싼 루브릭이 된다. (6.3)
> 7. **`부분` 상태를 `부분` / `확인필요`로 분리했다.** v2는 "의미가 불완전"과 "판정 확신이 낮음"을 한 상태에 담았다. 대응이 정반대(힌트 vs 확인 질문)라 로그에서 구분되지 않으면 개선율 지표가 오염된다.
> 8. **지표를 백분율에서 건수로 바꿨다.** v2는 10장에서 "거친 척도의 정밀 착시"를 이유로 %를 폐기해 놓고, 13장 지표는 전부 %였다. 표본 24건에서 4.2%p 단위로 움직인다. 같은 병이다.
> 9. **FP 목표의 문서 내 모순을 해소했다.** v2는 417행 "5% 이하"와 437행 "오판정이 나오면 출시하지 않는다"(=0건)를 동시에 말했다.
> 10. **판정 품질 목표를 로컬 모델 기준으로 재설정했다.** 판정은 사용자 브라우저가 부르는 로컬 모델이 수행한다. v2의 목표치는 그 제약을 모른 채 쓰였다.
> 11. **Phase 0을 후행으로 옮겼다.** 2~3일 · 참가자 6~8명 · 대조군은 제출 기한 안에 불가능하다. 계획으로 남기되 Phase 1이 선행한다.

---

## 1. 제품 개요

### 제품명
Mindi

### 한 줄 설명
학습자가 웹 개발 개념을 자기 말로 설명하면, **사전에 저작된 루브릭**을 기준으로 어떤 요소를 놓쳤고 무엇을 잘못 이해했는지 진단하고, 정답을 대신 제공하지 않는 단계별 힌트로 스스로 수정하게 돕는 학습 도구.

### 이 문서가 지키는 것과 바꿀 수 있는 것

**바꿀 수 없는 약속** — 3장(성공 상태), 7장(검색의 역할), 11장(응답 원칙), 14장(수용 기준).
**실험 대상** — 루브릭 요소 구성, 힌트 문구, 검색 파라미터, 모델. (20장)

---

## 2. 문제 정의

### 2.1 배경

생성형 AI로 모르는 개념의 설명을 즉시 얻을 수 있게 되면서, 학습자는 "설명을 읽고 이해했다고 느끼는 상태"에 매우 쉽게 도달한다. 그러나 이해했다는 느낌과 실제로 설명할 수 있는 능력은 일치하지 않는다.

자기 설명(self-explanation)과 인출 연습(retrieval practice)이 이 간극을 드러내고 좁힌다는 것은 교육심리학에서 반복 검증된 결과다. 문제는 이 두 활동이 **인지적으로 부담이 크고, 학습자가 자발적으로는 거의 하지 않는다**는 점이다.

### 2.2 해결하려는 문제

학습자가 웹 개발 개념을 실제로 이해했는지 스스로 확인하기 어렵고, 확인 활동(자기 설명)은 혼자서는 지속되지 않는다.

1. 읽고 이해한 느낌과 설명할 수 있는 상태가 다르며, 학습자는 이 차이를 인지하지 못한다.
2. 자기 설명은 부담이 크므로 외부 유도 장치 없이는 발생하지 않는다.
3. 일반 AI 챗봇은 정답을 먼저 제공하기 때문에 인출 기회를 구조적으로 없앤다.
4. 오개념은 학습자 스스로 발견하기 가장 어렵다. 누락은 "모른다"는 느낌을 주지만, 오개념은 "안다"는 느낌을 준다.
5. 다시 학습해야 할 부분을 찾기 위해 긴 원문을 재탐색하는 비용이 든다.

### 2.3 다루지 않는 문제

- 개념을 처음 가르치는 것(교수 기능). 본 제품은 학습 **이후** 단계만 담당한다.
- 코드 작성 능력이나 문제 해결 능력의 평가.

---

## 3. 성공 상태 (단일 정의)

하나의 개념에 대해 다음 두 조건을 **모두** 만족하면 `이해`로 본다.

1. 루브릭의 **필수 요소 전체**가 `이해` 상태다.
2. 판정된 **오개념이 없다**.

점수, 백분율, 등급은 사용하지 않는다. 문장 일치나 원문 암기는 판단 기준이 아니다.

### 3.1 소진 종료의 처리 (v3 명시)

v2는 F4의 힌트 소진(3회 후에도 미도달) 뒤 개념을 종료시키면서, **그 개념이 성공인지 실패인지 적지 않았다.** 흐름도에서 성공 경로와 소진 경로가 같은 상자로 들어갔다. Phase 0의 "개념 완주율"이 여기에 달려 있으므로 정의한다.

| 종료 유형 | 조건 | `이해` 여부 | 완주율 계수 |
|---|---|---|---|
| **달성 종료** | 필수 전체 `이해` + 오개념 없음 | ○ | ○ |
| **소진 종료** | 3회 재설명 후에도 미충족. 정답 요약 제공 | ✗ | ○ (끝까지 진행했으므로) |
| **이탈** | 재설명 없이 중단 | ✗ | ✗ |

완주율은 **달성 + 소진**을 센다. 이해율은 **달성만** 센다. 두 지표를 분리한다.

---

## 4. 타깃 사용자

### Primary User
웹 개발을 처음 배우는 비전공 초급 학습자 중, **강의·부트캠프·스터디 등 진행 중인 학습 맥락이 있는 사람.**

### 특징
- 웹 개발 용어가 아직 익숙하지 않다.
- 학습 자료를 읽지만 제대로 이해했는지 확신이 없다.
- 모르는 것이 생기면 AI에게 바로 질문하는 습관이 있다.
- 긴 설명을 반복해 읽는 것보다 필요한 부분만 다시 확인하고 싶어 한다.

### 이 타깃에 내재된 긴장

네 번째 특징과 본 제품의 핵심 메커니즘(정답을 바로 주지 않는다)은 **정면으로 충돌한다.** 제품이 성립하려면 다음 중 하나가 필요하다.

- (a) 학습자가 스스로 자기 설명의 가치를 인정하고 부담을 감수한다, 또는
- (b) 학습 맥락(강사, 커리큘럼, 스터디)이 이 활동을 지정한다.

(a)가 약하면 자발적 사용 도구가 아니라 **커리큘럼에 배치되는 도구**로 포지셔닝해야 하며, 이때 성공 지표는 자발적 재방문률이 아니라 지정 상황에서의 완주율이 된다. Phase 0(18장)이 이것을 측정한다.

---

## 5. 대체재

가장 현실적인 대체재는 **범용 챗봇에 프롬프트를 붙여넣는 것**이다. "내 설명을 평가하고 정답은 알려주지 말고 힌트만 줘" 한 줄로 유사한 경험을 무료로 만들 수 있다.

차별 요소는 셋이며, 각각 검증 대상이다.

| 차별 요소 | 내용 | 검증 |
|---|---|---|
| **판정 일관성** | 사전 저작된 루브릭을 쓰므로 같은 답변에 같은 판정이 나온다. 범용 챗봇은 세션마다 기준이 흔들린다 | 15장 · 동일 답변 3회 투입 시 요소별 일치 |
| **힌트 규율** | 프롬프트만으로는 LLM이 곧 정답을 흘린다. 힌트 단계를 시스템으로 강제한다 | 15장 · 정답 조기 노출 건수 |
| **근거 추적** | 모든 판정에 사용자 발화 구간 + 루브릭 요소 ID + MDN 청크 ID가 붙는다 | 14장 수용 기준 |

> **이 세 가지 모두 "루브릭이 미리 있다"에서 나온다.** 검색에서 나오지 않는다. 그래서 7장이 필요하다.

---

## 6. 콘텐츠 범위

### 6.1 MVP 개념 (2개)

| 개념 | MDN 근거 문서 (한국어판, 200 확인) | 선정 이유 |
|---|---|---|
| **A. HTTP** | `/ko/docs/Web/HTTP/Guides/Overview` | 요소 경계가 명확해 루브릭 신뢰도를 먼저 확보할 수 있다 (기술 검증용) |
| **B. CSS 박스 모델** | `/ko/docs/Learn_web_development/Core/Styling_basics/Box_model` | "왜 이 크기가 되는가"는 초급자 좌절 1순위 (수요 검증용) |

두 그룹에서 하나씩 남긴 이유는, 개념 선정 자체가 데이터로 재검토되어야 하기 때문이다(Risk 1). 한 그룹만 남기면 비교할 대상이 없다.

### 6.2 제외한 것과 그 이유

| 제외 | 이유 |
|---|---|
| **웹페이지 표시 과정** (v2 A3) | **근거 문서가 존재하지 않는다.** 6.3 참조 |
| 클라이언트/서버 (v2 A1) | HTTP 루브릭의 요소로 흡수. 단독 자기 설명 과제로는 밀도가 낮다 |
| 일반 흐름·레이아웃 (v2 B2), 이벤트 (v2 B3) | 루브릭 저작 비용. Group B 저작 방법이 검증된 후 도입 |
| 비동기 JavaScript | "어디까지 설명하면 이해인가"의 경계가 모호(이벤트 루프? 마이크로태스크 큐?)해 루브릭 신뢰도를 확보할 수 없다 |

### 6.3 원문 검증 기록 (v3 신규)

루브릭을 저작하기 전에 근거 문서와 섹션 앵커가 실재하는지 확인했다. **이 확인 없이 저작하면 존재하지 않는 근거에 요소를 매다는 일이 생긴다.**

**발견 1 — v2 A3의 근거 문서가 없다**

```
404  /Learn_web_development/Howto/Web_mechanics/What_happens_when_you_navigate_to_a_URL
```

MDN에 해당 제목의 단일 문서가 없다. 내용은 `How the web works`, `What is a URL?`, `What is a domain name?` 세 문서에 흩어져 있다. v2는 "DNS·URL을 A3에 흡수해 저작을 줄였다"고 했으나, 실제로는 **3문서 병합 루브릭이 되어 가장 비싼 개념**이 된다. 제외한다.

**발견 2 — v2 예시 루브릭의 `프로토콜` 요소는 원문에 근거가 없다**

v2 10장은 `프로토콜 — 통신의 규칙·약속이라는 성격 언급`을 **필수**로 두었다. 그러나 MDN 한국어판 HTTP 개요는 프로토콜을 정의하지 않는다. 도입부는 이렇게만 쓴다.

> "HTTP는 HTML 문서와 같은 리소스들을 가져올 수 있도록 해주는 프로토콜입니다."

학습자가 원문을 성실히 읽고 설명해도 "규칙·약속"이라는 표현은 나오지 않는다. **원문에 없으니까.** 그런데 필수 요소라 `미도달` 판정을 받는다. 이것은 Risk 2(맞는 설명을 틀렸다고 판정)의 발생 경로이며, "루브릭은 원문에서 추출한다"는 문서 자신의 원칙 위반이다.

원문에 실제로 있는 문장은 이것이다.

> "클라이언트-서버 프로토콜이란 (보통 웹브라우저인) 수신자 측에 의해 요청이 초기화되는 프로토콜을 의미합니다."

→ 판정 기준을 **"요청이 클라이언트 쪽에서 시작된다는 성격"**으로 교체했다(10.3).

**교훈: 루브릭 요소를 쓰기 전에 그 요소를 지지하는 원문 문장을 먼저 인용한다.** 인용할 문장이 없으면 그 요소는 필수가 될 수 없다. 이 절차를 6.4에 넣었다.

**확인된 앵커** (전부 실측)

| 개념 | 요소 | 앵커 |
|---|---|---|
| HTTP | 클라이언트 | `#클라이언트_사용자_에이전트` |
| HTTP | 서버 | `#웹_서버` |
| HTTP | 요청 | `#요청` |
| HTTP | 응답 | `#응답` |
| HTTP | 관계 서술 | `#http_흐름` |
| HTTP | 상태 비저장 (선택) | `#http은_상태는_없지만_세션은_있습니다` |
| 박스 모델 | 박스 구성 | `#box의_구성` |
| 박스 모델 | 표준 모델 | `#표준_css_박스_모델` |
| 박스 모델 | 대체 모델 | `#대체_css_박스_모델` |
| 박스 모델 | 여백·테두리·패딩 | `#여백` `#테두리` `#패딩` |

### 6.4 루브릭 저작 프로세스

제품 품질은 대부분 LLM이 아니라 **루브릭 저작 품질**에서 나온다(Risk 3). 저작을 명시적 작업으로 관리한다.

1. **원문 인용 먼저.** 요소 하나마다 그것을 지지하는 원문 문장을 먼저 뽑는다. 뽑을 문장이 없으면 필수 요소가 될 수 없다. (6.3 발견 2)
2. **앵커 실측.** 문서 URL과 섹션 앵커가 200으로 응답하는지 확인한다.
3. **1인 저작 + 24시간 후 self-review.** v2는 2인 독립 저작을 요구했으나 1인 프로젝트에서 성립하지 않는다. 시간 간격을 두고 다시 읽어 필수/선택 구분이 흔들리면 **필수에서 제외한다**(보수적 원칙은 유지).
4. **예상 오개념은 추정으로 시작하고 실제 답변으로 교체한다.** 추정 오개념은 실제 분포와 자주 다르다.
5. **판정 오류가 나오면 프롬프트가 아니라 루브릭을 먼저 고친다.** 루브릭은 버전 관리한다.

### 6.5 Group B의 입력 형식 (v3 수정)

박스 모델을 넣은 이유는 "초급자가 실제로 막히는 지점"이다. 그런데 그 좌절은 **구체적인 코드와 화면 앞에서** 일어난다. 맥락 없이 "박스 모델을 설명해 보세요"라고 물으면 HTTP와 똑같은 추상 설명 과제가 되어 **선정 이유 자체가 무효화된다.**

따라서 Group B 개념은 **짧은 코드 스니펫과 예상 결과를 함께 제시하고** "왜 이렇게 되는지 설명해 보세요"로 묻는다. 코드 실행은 여전히 필요 없다(정적 텍스트).

```css
.box { width: 300px; padding: 20px; border: 5px solid; }
```
> 이 요소가 화면에서 차지하는 가로 폭은 350px입니다. 왜 그런지 설명해 보세요.

---

## 7. 검색은 왜 있는가 (v3 신규 — 이 문서의 핵심 절)

이 제품의 핵심은 **루브릭 채점**이다. 그렇다면 임베딩과 검색은 왜 필요한가. 답하지 않으면 검색은 장식이 된다.

### 7.1 먼저, 검색을 쓰지 않는 곳

> **요소별 상태 판정에는 검색을 쓰지 않는다. 판정은 오직 루브릭으로만 한다.**

검색 결과를 판정 근거로 쓰면 같은 답변이 검색 결과에 따라 다르게 채점된다. 판정 일관성은 범용 챗봇 대비 **유일한 구조적 우위**(5장)이므로 양보하지 않는다. 이 선은 11장 원칙 1과 같은 말이다.

### 7.2 검색이 실제로 일하는 세 자리

**① 범위 판정과 거절 — 검색으로만 가능하다 (MVP에서 필수)**

루브릭은 *이미 고른 개념*의 요소 목록이다. 사용자가 이렇게 물었을 때,

> "왜 내 div가 안 움직여요?"

이것이 자료 범위 안인지 밖인지 **루브릭은 답할 수 없다.** 개념 이름이 나오지 않기 때문이다. 하드코딩된 개념 목록 매칭도 실패한다. MDN 청크에 대한 유사도 검색과 임계값만이 이 판정을 한다.

- 유사도가 임계값 이상 → 범위 안. 해당 개념으로 안내
- 임계값 미만 → 범위 밖. 거절 (F8)

이것이 `refusal` 판정의 근거이며, **루브릭으로 대체 불가능하다.**

**② 오개념 교정의 출처 — Risk 6 방어 (MVP에서 필수)**

"서버가 요청을 보낸다"가 오개념이라고 말할 때, **왜** 그런지는 루브릭에 없다. 루브릭은 판정 기준이지 설명이 아니다. 모델이 자기 지식으로 설명을 채우면 Risk 6(루브릭 범위를 벗어난 지식 사용)이 그대로 발생한다.

교정 문장은 **검색된 MDN 청크에 근거해야 하고, 그 청크 ID가 화면에 남는다.** 이것이 `grounded`·`cited`의 근거다.

**③ 힌트 3의 문단 선택 (MVP에서는 이득이 작다 — 정직한 한계)**

루브릭은 요소당 앵커 하나를 가진다. 그러나 같은 요소를 놓쳐도 학습자마다 막힌 지점이 다르므로, 질의를 `미도달 요소명 + 사용자 발화`로 두면 그 학습자에게 맞는 문단이 선택된다.

> **한계를 그대로 적는다.** 개념 2개 · 청크 30개 규모에서는 ③이 고정 앵커 대비 이득이 크지 않다. 고정 앵커로도 거의 같은 문단이 나온다. ③은 개념이 늘어날 때를 위한 자리이며, **MVP에서 검색을 정당화하는 것은 ①과 ②다.**

### 7.3 정리

| | 판정 기준 | 문장의 출처 | 범위 판정 |
|---|---|---|---|
| 담당 | **루브릭** | **검색된 MDN 청크** | **검색 유사도** |
| 검색을 쓰면 | 일관성이 깨진다 (금지) | — | — |
| 루브릭을 쓰면 | — | 설명이 없다 | 개념 밖을 모른다 |

두 장치는 **서로를 대체하지 못한다.** 이것이 이 제품에 둘 다 있는 이유다.

---

## 8. 핵심 기능

### F0. 원문 학습 진입 (P0)
개념 선택 시 해당 MDN 문서 링크와 3~5줄 요약을 함께 제시한다. 요약은 **검색된 청크로 구성하고 청크 ID를 남긴다**(7.2 ②와 같은 원칙). 사용자는 "이미 읽었어요"로 건너뛸 수 있다. 원문을 앱 안에 임베드하거나 재구성하지 않는다.

### F1. 개념 선택 (P0)
HTTP, 박스 모델 2개를 목록으로 제시한다. Group 구분은 사용자에게 노출하지 않는다.

### F2. 자기 설명 유도 (P0)
- 정답을 먼저 보여주지 않는다.
- 설명형 질문을 우선한다. 단 첫 설명 시도가 없으면 **1회 완화 질문**을 제공한다(예: "떠오르는 단어 두세 개만 적어도 괜찮아요"). 이후 설명형으로 복귀한다.
- Group B 개념은 코드 스니펫 + 예상 결과를 함께 제시한다(6.5).

### F3. 루브릭 기반 진단 (P0)
사용자 답변을 해당 개념의 루브릭과 대조한다.

**산출**
- 요소별 상태: `이해` / `부분` / `확인필요` / `미도달`
- 오개념: 탐지된 항목과 그 근거 문장
- 관계 서술 여부
- **각 판정마다**: 사용자 발화 구간 + 루브릭 요소 ID + MDN 청크 ID

의미로 판정한다. 표현이 달라도 의미가 정확하면 `이해`다.

### F4. 단계별 힌트 (P0)

| 단계 | 제공 범위 | 예시 |
|---|---|---|
| Hint 1 | 방향만 | "브라우저가 서버에 먼저 무엇인가를 보낸다는 점을 생각해 보세요." |
| Hint 2 | 관련 개념의 이름 | "이 통신에는 요청과 응답이라는 두 개념이 있습니다." |
| Hint 3 | MDN 원문의 해당 문단 | 링크 + 검색된 청크 (7.2 ③) |

**종료 조건**
- 개념 1개당 재설명 최대 3회.
- Hint 3 이후 재설명에서도 필수 요소가 미도달이면 **정답 요약을 제공하고 소진 종료**한다(3.1). 미도달 요소는 "다시 볼 항목"으로 남긴다.
- 이 규칙이 없으면 무한 루프가 되고, 이 지점이 좌절 이탈의 최대 발생 구간이다.

### F5. 재설명 (P0)
힌트 후 다시 설명하면 F3을 재실행한다. 이전 판정을 함께 보여주어 무엇이 바뀌었는지 알 수 있게 한다.

### F6. 요소별 결과 표시 (P0)

백분율과 등급을 쓰지 않는다.

```
HTTP

이해한 요소
  ✓ 클라이언트 — "브라우저가 요청을 보낸다"           [RB-A1] [MD-003]
  ✓ 서버 — "서버가 받아서 돌려준다"                    [RB-A2] [MD-005]
  ✓ 요청                                              [RB-A3] [MD-011]

다시 볼 요소
  △ 응답 — 언급은 있으나 서버가 반환한다는 설명이 없음  [RB-A4] [MD-012]
  ? 요청 시작 주체 — "신호를 보낸다"가 요청을 뜻하는지 확인 필요
  ✗ (없음)

관계 서술
  ✓ 요청과 응답의 순서를 설명함                        [RB-A6] [MD-009]

바로잡을 내용
  없음
```

각 항목에서 MDN 원문의 해당 위치로 이동할 수 있다.

### F7. 원문으로 돌아가기 (P1)
미도달 요소별로 MDN 원문의 해당 섹션 링크를 제공한다.

### F9. 이의 제기 (P0, v4 신규)

**초급 학습자는 판정 이유를 검증할 능력이 없다**(Risk 2). 이유를 보여 주는 것만으로는 부족하고, 아니라고 말할 자리가 있어야 한다.

다만 이 자리가 채점을 우회하는 통로가 되면 안 된다. 그래서 기준을 하나로 좁힌다.

> **원래 설명 안에 이미 그 뜻이 있었는가.**

| 경우 | 처리 |
|---|---|
| 있었다 | 진단이 놓친 것이다. 그 요소를 `이해`로 고치고, 근거 구간을 인용으로 남긴다 |
| 해명에서 처음 나왔다 | 이의가 아니라 재설명이다. 그 부분을 넣어 다시 말하도록 안내한다 |

**세 겹으로 막는다.**

1. **프롬프트** — 근거 구간만 따로 읽어도 판정 기준을 만족해야 한다
2. **코드** — 근거 구간이 원래 발화의 실제 부분 문자열이 아니면 받아들임을 되돌린다 (요소 판정과 같은 검사)
3. **상한** — 한 턴에 2회까지. 없으면 모든 요소에 이의를 걸어 달성으로 밀 수 있다

**받아들여진 이의는 루브릭이 모호했다는 증거다.** 판정만 고치고 지나가면 같은 곳에서 계속 걸리므로, 요소별로 집계해 **루브릭 개선 로그**로 읽는다(6.4 원칙 5). 같은 요소에서 이의가 2건 이상 받아들여지면 그 판정 기준을 검토한다.

---

### F8. 범위 밖 처리와 거절 (P0, v3 신규)

v2 흐름에는 이 분기가 없었다. 실제로는 가장 자주 일어난다.

| 유형 | 예시 | 응답 |
|---|---|---|
| **범위 밖 개념** | "React useEffect 설명해줘" | 유사도가 거절 구간임을 근거로, 현재 다루는 개념 2개를 안내하고 답하지 않는다 |
| **정답 요구** | "그래서 HTTP가 뭔데요? 그냥 알려주세요" | 유사도와 무관하게 원칙 2에 따라 거절하고 힌트 단계로 되돌린다. **단 F4 종료 조건에 도달했으면 제공한다** |
| **경계 근처** | "왜 내 div가 안 움직여요?" | 확인 구간이면 단정하지 않고 되묻는다 |

거절할 때도 **왜 범위 밖인지의 근거**(최고 유사도와 대조한 청크)를 화면에 남긴다. 근거 없는 거절은 사용자가 검증할 수 없다.

#### 범위 판정 구간 (실측으로 확정)

단일 임계값으로는 이 판정을 할 수 없다는 것이 측정에서 드러났다. **원칙 7(저확신 시 단정하지 않고 확인 질문)을 범위 판정에도 그대로 적용해** 세 구간으로 나눈다.

| 구간 | 유사도 | 처리 |
|---|---|---|
| **안** | ≥ 0.65 | 진단으로 진행 |
| **확인** | 0.58 ~ 0.65 | 단정하지 않고 "이건 박스 모델 이야기인가요?"로 되묻는다 |
| **밖** | < 0.58 | 거절. 다루는 개념 2개를 안내 |

**측정 근거** (`npm run probe`, 탐침 14건, 청크 47개)

| 무리 | 유사도 범위 |
|---|---|
| 자료 안 6건 | 0.705 ~ 0.806 |
| 경계 2건 | 0.573 ~ 0.640 |
| 자료 밖 6건 | 0.453 ~ 0.553 |

자료 안 최저(0.705)와 자료 밖 최고(0.553) 사이 **분리 여유 0.152**. 두 무리가 겹치지 않으므로 **7.2 ①의 주장이 실측으로 성립한다.**

> **애초의 0.55는 틀린 값이었다.** "깃 머지랑 리베이스 차이"(0.553)가 범위 안으로 통과한다. 일반 RAG 챗봇에서 쓰던 약한 근거 임계값을 그대로 가져온 결과였고, 측정하지 않았으면 몰랐을 오류다.
>
> **표본이 14건이라는 점을 숨기지 않는다.** 구간 경계는 개념이나 청크가 바뀌면 다시 재야 한다. `npm run probe`가 회귀 검사 역할을 한다.

---

## 8-A. 화면 구성

페이지는 넷이고, 셋은 한 화면 안에서 단계로 바뀐다. 라우팅을 두지 않는 이유는
학습이 한 자리에서 끝나야 하기 때문이다. 뒤로 가기로 진행이 사라지면 안 된다.

| # | 화면 | 언제 | 무엇이 있나 | 무엇을 두지 않나 |
|---|---|---|---|---|
| 1 | **첫 화면** | 처음 | 이름 한 줄, 소개 한 줄, 개념마다 버튼 둘(설명해 보기 · 읽어 보기) | 루브릭 버전, 요소 개수 같은 내부 용어 |
| 2 | **읽기** | 읽어 보기 | 문단마다 키워드 + 본문, 가리기/보이기, 원문 링크, 나가는 문(이걸로 충분해요) | 채점으로 가는 강제 |
| 3 | **설명** | 설명해 보기 | 질문, 입력창, 턴 카드 목록, 접힌 코드 예 | 채점표 전체 |
| 4 | **옆 칸** | 2·3 화면 | 진행 점과 숫자, 접힌 설정과 기록, 내려받는 중일 때만 진행률 | 요소 이름·내부 ID |

### 8-A.1 턴 카드에 담기는 것과 접는 것

한 번 답한 뒤 화면에 남는 것이 이 제품의 약속이 지켜지는 자리다.

| 자리 | 기본 | 이유 |
|---|---|---|
| 내가 쓴 설명 | 펼침 | 무엇에 대한 진단인지 |
| N가지는 잘 설명했어요 | 펼침(목록은 접힘) | 한 번에 하나만 고칠 수 있다 |
| 바로잡을 내용 | 펼침 | 오개념은 방치 비용이 가장 크다 |
| 다음 한 가지 | 펼침 | 힌트 1단계에서는 **이름을 쓰지 않는다** |
| 힌트·정리 | 펼침(스트리밍) | 읽는 글 |
| **이 답이 본 자료 N개** | **접힘** | 출처는 있어야 하되 먼저 들이밀지 않는다 |
| **이 진단 자체 검사** | **접힘** | 아래 8-A.2 |
| 👍 👎 | 펼침 | 자동 판정과 어긋나는 지점이 실험 재료다 |
| **이건 말했는데요** | 요소 목록 안 | 미도달 요소에만. 진단이 틀릴 수 있다 (F9) |

### 8-A.2 자동 판정을 접어 두는 이유 (사용자 관찰에서 나옴)

처음에는 여섯 필드를 배지로 늘어놓았다. 사용자가 말했다.

> "하나라도 빨간불이 뜨면 이 내용 전체에 대한 신뢰도가 없어져."

맞는 말이다. 그러나 지우는 것도 답이 아니다. **문장이 매끄럽다는 사실과 근거가
있다는 사실은 다르고, 그 둘을 구분해 보여 주는 것이 이 제품의 약속이기 때문이다.**

그래서 접는다. 요약 한 줄(`믿을 만해요` / `한 군데 아쉬워요` / `이 진단은 의심스러워요`)만
두고, 펼치면 여섯 항목을 **영어 필드명이 아니라 질문 형태**로 보여 준다.
`grounded` 대신 "근거 안에서만 판단했나".

첫눈에 빨간불이 보이지 않으면 신뢰를 깎지 않고, 확인할 길은 남는다.

### 8-A.3 두지 않기로 한 것

- **백분율·등급·누적 점수** — 10.1
- **요소 ID(RB-A1)와 상태 코드** — 학습자의 언어가 아니다. 내부 식별자는 로그에만
- **시간 컨텍스트** — HTTP와 박스 모델은 시점에 무관한 개념이라 프롬프트에 넣지 않는다.
  자료가 시점을 타는 도메인(행사 일정 등)으로 바꾸면 그때 넣는다

---

## 9. 대화 흐름

```text
개념 선택 (F1)
      ↓
원문 링크 + 요약 제시 (F0)  ── "이미 읽었어요" ──┐
      ↓                                          │
설명형 질문 (F2)  ←────────────────────────────┘
      ↓
사용자 입력
      ↓
   ┌────────────────────────────────────────┐
   │ 범위 판정 (검색 유사도)  → 범위 밖 / 정답 요구 │ → 거절 + 근거 표시 (F8)
   └────────────────────────────────────────┘
      ↓ (범위 안)
[설명 없음 / 1문장 이하] → 완화 질문 1회 → 재시도
      ↓
루브릭 대조 진단 (F3)
      ↓
   ┌──────────────────────────────────┐
   │ 필수 전체 이해 + 오개념 없음        │ → 달성 종료 → 결과 표시 (F6)
   └──────────────────────────────────┘
      ↓ (미충족)
오개념이 있으면 오개념 먼저 교정 대상으로 선택 (근거: 검색 청크)
      ↓
Hint 1 → 재설명 (1회차)
      ↓ (여전히 미충족)
Hint 2 → 재설명 (2회차)
      ↓ (여전히 미충족)
Hint 3 (검색된 원문 문단) → 재설명 (3회차)
      ↓ (여전히 미충족)
정답 요약 + "다시 볼 항목" 등록 → 소진 종료 (F6)
```

---

## 10. 평가 모델

### 10.1 폐기한 것

v1의 2/1/0 점수 합산과 백분율 표시를 폐기한다.

1. **모델 충돌.** 이진 판정·12점 만점·3단계 라벨이 임계값 매핑 없이 공존했다.
2. **예시가 스펙을 통과하지 못했다.** "80%"는 6요소 12점 체계에서 산출 불가능하다(9.6점).
3. **거친 척도의 정밀 착시.** 6요소 × 2점은 약 8.3%p 단위로만 움직이는데 정수 %로 표시하면 정밀한 측정처럼 보인다. "보조 지표"라고 선언해도 사용자는 화면의 숫자를 성적으로 받아들인다. **선언으로 해결되지 않는다.**

> **v3 추가:** 이 논리는 15장 지표에도 그대로 적용한다. v2는 여기서 %를 폐기하고 지표는 전부 %로 썼다. 표본 24건에서 4.2%p 단위로 움직인다. 같은 병이다.

### 10.2 채택한 모델 — 요소별 4상태 체크리스트

| 상태 | 판정 조건 | 대응 |
|---|---|---|
| `이해` | 핵심 의미가 사용자 발화에 명시적으로 존재 | 없음 |
| `부분` | 언급은 있으나 핵심 의미가 불완전 | **힌트** |
| `확인필요` | 사용자 표현이 요소와 대응하는지 확신이 낮음 | **확인 질문** |
| `미도달` | 해당 요소가 나타나지 않음 | 힌트 |
| `오개념` | 요소와 모순되는 진술 (상태와 별도 표시) | 최우선 교정 |

> **v3 변경:** v2는 `부분`에 "불완전"과 "확신 낮음"을 함께 담았다. 대응이 정반대(힌트 vs 확인 질문)인데 상태가 같으면 로그에서 구분되지 않아, "힌트 후 개선율"이 오염된다. 확신이 낮았을 뿐인 건이 확인 질문으로 해소된 것까지 개선으로 계수되기 때문이다.

집계 점수는 산출하지 않는다. 흐름 제어(다음 개념으로 넘길지)는 3장의 정의를 쓰며, 이 판단은 내부에서만 쓰이고 등급으로 노출하지 않는다.

### 10.3 HTTP 루브릭 (v3 수정)

| ID | 요소 | 구분 | 판정 기준 | 지지 원문 | 앵커 |
|---|---|---|---|---|---|
| RB-A1 | 클라이언트 | 필수 | 요청을 보내는 주체로 서술 | "보통 브라우저인 클라이언트에 의해 전송되는 메시지를 요청이라 부르며" | `#클라이언트_사용자_에이전트` |
| RB-A2 | 서버 | 필수 | 요청을 받고 응답을 돌려주는 주체로 서술 | "서버에서 응답으로 전송되는 메시지를 응답이라고 부릅니다" | `#웹_서버` |
| RB-A3 | 요청 | 필수 | 클라이언트 → 서버 방향으로 서술 | 위와 동일 | `#요청` |
| RB-A4 | 응답 | 필수 | 서버 → 클라이언트 방향으로 서술 | 위와 동일 | `#응답` |
| **RB-A5** | **요청 시작 주체** | 필수 | **통신이 클라이언트 쪽에서 시작된다는 성격 언급** | **"클라이언트-서버 프로토콜이란 수신자 측에 의해 요청이 초기화되는 프로토콜을 의미합니다"** | `#클라이언트_사용자_에이전트` |
| RB-A6 | 관계 서술 | 필수 | 요청과 응답의 방향·순서 연결 | "개별적인 메시지 교환에 의해 통신합니다" | `#http_흐름` |
| RB-A7 | 상태 비저장 | 선택 | — | — | `#http은_상태는_없지만_세션은_있습니다` |
| RB-A8 | 헤더/메서드 | 선택 | — | — | `#http_메시지` |

> **RB-A5가 v2의 `프로토콜 — 규칙·약속`을 대체한다.** 교체 사유는 6.3 발견 2.

---

## 11. AI 응답 원칙

1. **루브릭 우선.** 요소 판정은 사전 저작된 루브릭을 기준으로 한다. 루브릭에 없는 항목은 평가 대상이 아니다. **검색 결과는 판정 근거가 아니다**(7.1).
2. **정답 즉시 제공 금지.** 틀렸다는 이유로 완성된 설명을 곧바로 제시하지 않는다. 단 F4 종료 조건에 도달하면 제공한다.
3. **최소 힌트 우선.** 사용자가 스스로 수정할 수 있을 만큼만 제공한다.
4. **표현이 달라도 의미가 같으면 `이해`.** 원문 문장과의 일치는 기준이 아니다.
5. **오개념 우선 교정.** 누락보다 잘못 이해한 내용을 먼저 다룬다. 오개념은 "안다"고 느끼는 상태이므로 방치 비용이 크다.
6. **근거 없는 오답 처리 금지.** 루브릭으로 판단할 수 없는 내용은 오답으로 처리하지 않는다.
7. **저확신 시 판정 보류.** 확신이 낮으면 `미도달`이 아니라 `확인필요`로 두고 확인 질문을 한다. 예: "말씀하신 '신호를 보낸다'가 요청을 뜻하는 건지 확인하고 싶어요." — **맞는 설명을 틀렸다고 판정하는 것은 초급자에게 없던 오개념을 주입하는 경로**이므로, 놓치는 것보다 비용이 크다.
8. **이의 제기 경로 제공.** 모든 판정에 "이렇게 이해했는데 왜 아닌가요?" 경로를 둔다. 반박이 타당하면 판정을 수정하고 루브릭 개선 로그에 기록한다.
9. **교정과 요약은 검색된 청크에 근거한다 (v3).** 오개념 교정 문장, F0 요약, Hint 3은 모델의 자체 지식이 아니라 검색된 MDN 청크에서 나오고, 청크 ID를 남긴다. 근거를 찾지 못하면 교정하지 않고 확인 질문으로 돌린다.

---

## 12. 판정은 두 층이다 (v3 신규)

이 제품에는 **대상이 다른 판정이 두 개** 있다. 문서와 화면에서 구분하지 않으면 읽는 사람이 섞는다.

| | **1층: Mindi 판정** | **2층: 자동 판정 (LLM-as-a-Judge)** |
|---|---|---|
| 무엇을 채점하나 | **사용자의 설명** | **Mindi의 진단 메시지** |
| 기준 | 루브릭 | 근거성 |
| 출력 | 요소별 4상태 + 오개념 | 6필드 + verdict |
| 사용자에게 | 학습 피드백 | 이 진단을 믿어도 되는지의 신호 |

### 2층 6필드의 정의 (이 제품에서의 의미)

| 필드 | 이 제품에서 무엇을 보는가 |
|---|---|
| `grounded` | 진단이 **루브릭 요소와 검색된 청크 안에서만** 근거를 댔는가. 루브릭에 없는 기준을 지어내지 않았는가 (Risk 6) |
| `cited` | 각 판정에 **사용자 발화 구간 + 루브릭 요소 ID + MDN 청크 ID**가 붙었는가 |
| `refusal` | 범위 밖 개념 / 정답 요구를 **옳게 거절**했는가. 범위 안 질문을 거절했으면 실패 |
| `relevant` | 사용자가 **실제로 말한 것**에 대한 진단인가. 말하지 않은 것을 지적하지 않았는가 |
| `complete` | 루브릭의 **필수 요소 전부**를 판정했는가. 빠뜨린 요소가 없는가 |
| `verdict` | `pass` / `warn` / `fail` 종합 |

> `complete`가 이 제품에서 특히 중요하다. 필수 요소 6개 중 4개만 판정하고 넘어가면 사용자는 나머지 2개를 이해했다고 오해한다.

---

## 13. MVP 범위

### 포함
- 개념 2개 (HTTP, 박스 모델) + 각 루브릭 (필수/선택 요소, 지지 원문, 앵커, 예상 오개념)
- MDN 청크 벡터스토어 (범위 판정 · 교정 근거 · Hint 3용)
- 원문 링크 + 청크 기반 요약 (F0)
- 설명형 질문 및 완화 질문, Group B 코드 스니펫
- 루브릭 대조 진단 (요소별 4상태 + 오개념)
- 3단계 힌트 및 종료 규칙, 재설명 최대 3회
- 요소별 결과 표시 + 판정 근거 표시 + 이의 제기 경로
- **범위 밖 처리와 거절 (F8)**
- **2층 자동 판정 6필드**

### 제외
- 전체 MDN 검색, 복수 출처 통합
- 백분율·등급·누적 점수
- 원문 앱 내 임베드/재구성
- 경계 모호 개념 (비동기 JS 등)
- 사용자 장기 학습 기록, 자동 커리큘럼, 개인화 추천
- 코드 실행 (스니펫은 정적 텍스트)
- 사용자 간 비교

---

## 14. 수용 기준

배포 주소에서, Ollama가 실행 중인 브라우저로 **아래 순서대로** 확인한다.
루브릭이나 자료가 바뀌면 같은 순서로 다시 돌리고 결과를 실험 표에 남긴다.

### 14.1 기능 수용 기준

| # | 유형 | 입력 | 통과 기준 |
|---|---|---|---|
| A1 | 완전한 설명 | HTTP 필수 6요소를 모두 담은 설명 | 전부 `이해`, 달성 종료, `verdict = pass` |
| A2 | 표현 변형 정답 | 같은 의미를 다른 말로 ("브라우저가 먼저 말을 걸고 서버가 대답한다") | 전부 `이해`. **하나라도 `미도달`이면 실패**(FP) |
| B1 | 부분 누락 | 요청·응답만 언급, 시작 주체 없음 | RB-A5가 `미도달`, Hint 1 제공, 정답 미노출 |
| B2 | 오개념 | "서버가 브라우저에 요청을 보낸다" | 오개념 탐지, **교정 문장에 청크 ID가 붙음**, 오개념 우선 처리 |
| C1 | 저확신 | "뭔가 신호를 주고받아요" | `확인필요` + 확인 질문. `미도달`로 단정하지 않음 |
| D1 | 범위 밖 | "React useEffect 설명해줘" | 거절, `refusal` O, **최고 유사도를 근거로 표시** |
| D2 | 정답 요구 | "그냥 답 알려주세요" | 거절 후 힌트 단계 복귀, 정답 미노출 |
| E1 | 힌트 소진 | 3회 연속 미충족 | 정답 요약 제공, 소진 종료, 무한 루프 없음 |
| F1 | 이의 제기 | 미도달 요소에 "이건 말했는데요" | 정당한 이의는 판정을 고치고, 원래 설명에 없던 내용은 재설명으로 안내. 한 턴 2회 상한 |

### 14.2 시스템 수용 기준

- [ ] 배포 주소에서 자산이 모두 로드된다 (`chunks.json`, `rubrics/*.json` 포함, 404 없음)
- [ ] 첫 방문 임베딩 다운로드 진행률이 화면에 보인다
- [ ] Ollama가 꺼져 있으면 연결 실패 안내와 재시도 방법이 보인다
- [ ] 생성 중 중지 버튼이 실제로 생성을 끊는다
- [ ] 모든 진단에 2층 판정 배지가 남거나, 판정 실패가 명시된다
- [ ] **모든 요소 판정에 사용자 발화 구간 + 루브릭 요소 ID가 붙는다** (청크 ID는 교정·힌트·요약에만)

---

## 15. 성공 지표

지표를 **행동**(제품이 쓰이는가)과 **품질**(판정이 믿을 만한가)로 분리한다.

### 15.1 품질 지표 (Phase 1 — 제출 대상)

**모든 목표를 건수로 쓴다**(10.1 논리의 일관 적용). 표본이 작을 때 %는 정밀 착시를 만든다.

| 지표 | 정의 | 표본 | 목표 | **1차 측정** |
|---|---|---|---|---|
| **오판정 (FP)** | 표현 변형 정답을 `미도달`/`오개념`으로 판정 | 표현변형 셀 **6건** | **0건** (상한 제약) | **0건** ✅ |
| 오개념·누락 탐지 누락 | 결함이 있는데 달성으로 통과시킨 것 | 부분+오개념 셀 **8건** | 2건 이하 | **0건** ✅ |
| 오개념 탐지 | 심어 둔 모순을 잡았는가 | 오개념 셀 **4건** | 3건 이상 | **4건** ✅ |
| 범위 밖 거절 | 자료 밖 입력을 거절했는가 | **2건** | 2건 | **2건** ✅ |
| 판정 일관성 | 동일 답변 3회 투입 시 요소별 판정 불일치 | 3문항 **16칸** | 측정 후 기준 설정 | 1~3차 0칸 → **4차 4칸** (README 실험 10) |
| 근거 누락 | 발화 구간 또는 루브릭 요소 ID가 빠진 판정 | 전체 판정 | 0건 | 코드로 강제 |
| 정답 조기 노출 | Hint 1·2에 필수 요소의 정답 표현이 직접 등장 | 힌트 제공 전체 | **0건** (상한 제약) | 미측정 |
| 힌트 후 실질 개선 | 재설명에서 상태가 개선된 건. **힌트 문구를 그대로 반복한 사례는 제외** | 힌트 후 재설명 | 측정 후 기준 설정 | 미측정 |

> **1차 측정 = 고정 세트 v1.0.0 · qwen3:4b · 2026-08-28.** 원자료는 `measurement.json`, 실행은 `npm run measure`.
> 목표를 통과하기까지 세 번 측정했고, 그 사이에 무엇을 바꿨는지는 README 실험 5에 남겼습니다.
> **판정 일관성 0칸은 15.2가 예상한 것보다 좋습니다.** 다만 표본 16칸이므로 이것으로 목표치를 확정하지 않습니다.

**FP 목표를 0건으로 고정한 이유 (v2 모순 해소).** v2는 417행에서 "5% 이하", 437행에서 "오판정이 나오면 출시하지 않는다"(=0건)를 동시에 말했다. 표본 24건에서 1건은 4.2%다. 즉 "5% 이하"는 실질적으로 "1건 이하"인데 437행은 0건을 요구했다. **0건으로 통일한다.** FP는 최적화 대상이 아니라 상한 제약이다.

**트레이드오프 고정.** 힌트 후 개선율과 정답 조기 노출은 상충한다(힌트를 강하게 하면 둘 다 오른다). **정답 조기 노출 0건을 제약으로 고정하고, 그 안에서 개선을 최대화한다.** 개선율을 위해 노출을 희생하지 않는다.

### 15.2 로컬 모델 제약 (v3 신규)

판정은 서버가 아니라 **사용자 브라우저가 부르는 로컬 모델**이 수행한다. 이 문서의 목표치는 그 제약 안에서 읽어야 한다.

**1차 측정 전에 적었던 것**: `qwen3:4b`, temperature 0에서도 판정 근거 문구가 실행마다 달라지므로, 요소 6개 × 4상태의 일치율은 낙관하기 어렵다. 따라서 목표치는 측정 후 확정한다.

**1차 측정 후 관찰**: 판정 근거 *문구*는 실행마다 달라지지만, **요소별 상태는 3회 모두 같았다**(16칸 불일치 0). `temperature 0` + `format: "json"` + 구조화된 루브릭이 상태 판정을 안정시킨 것으로 보인다. 예상이 틀렸고, 그 사실을 기록으로 남긴다.

**목표 미달 시의 원칙은 그대로 유지한다.** 미달이면 목표를 낮추는 것이 아니라 원인을 먼저 가른다.

- 루브릭 모호성이면 → 루브릭 수정 (6.4 원칙 5)
- 진단기가 자기 판정과 모순되면 → **코드로 검사**한다. 1차 측정의 FP 5/6은 프롬프트를 고쳐서가 아니라, 모델의 오개념 출력을 모델 자신의 요소 판정으로 검사해 0/6이 되었다 (README 실험 5)
- 모델 한계면 → 상위 모델 권장을 README에 명시하고, 기본 모델에서의 실제 수치를 함께 공개

### 15.3 행동 지표 (Phase 0 — 후행)

18장 참조. 이 지표가 통과하지 못하면 품질 지표는 의미가 없다. 다만 **순서상 Phase 1이 먼저다**(18.1).

---

## 16. 고정 답변 세트와 실험 설계

과제의 "고정 질문 세트"에 대응하는 것이, 이 제품에서는 **고정 학습자 답변 세트**다. 입력이 질문이 아니라 설명이기 때문이다.

### 16.1 세트 구성 (개념 1개당 9건)

| 유형 | 건수 | 목적 |
|---|---|---|
| 완전한 정답 | 1 | 기준선 |
| **표현 변형 정답** | **3** | **FP 전용 셀. 다른 지표가 좋아도 여기서 오판정이 나오면 출시하지 않는다** |
| 부분 누락 | 2 | 힌트 경로 |
| 오개념 | 2 | 오개념 우선 교정 + 교정 근거 |
| 범위 밖 | 1 | 거절 경로 (`refusal`) |

개념 2개 × 9건 = **18건.** v2의 120건은 제출 기한 안에 라벨링할 수 없다.

### 16.2 라벨링

- 1인 라벨링 + 24시간 후 self-review(6.4와 동일 원칙).
- 라벨이 흔들리는 항목은 **루브릭 기준 자체가 모호한 것**으로 보고 루브릭을 먼저 고친다. v2의 Cohen's kappa 0.7 기준은 2인 라벨링 전제라 1인 프로젝트에서는 계산되지 않는다.

### 16.3 실험 기록 형식

한 번의 실행에서는 **변수 하나만** 바꾸고 나머지 조건과 입력 순서를 유지한다. 실패한 결과도 지우지 않는다.

| 항목 | 내용 |
|---|---|
| 실험 이름 | |
| 바꾼 변수 | (하나만) |
| 고정 조건 | 모델 · 임계값 · topK · 루브릭 버전 |
| 결과 표 | 입력 9건 × (요소별 상태, 2층 6필드) |
| 관찰한 근거 | |
| 결정 | 채택 / 되돌림 — 이유 |

---

## 17. 위험 요소

### Risk 1. 사용자가 자기 설명을 시도하지 않는다 (최상위)

타깃은 "AI에게 바로 질문하는" 습관을 가진 초급자이고, 자기 설명은 인지 부담이 크다. 특히 HTTP는 몰라도 진행이 멈추지 않으므로 **지금 노력할 동기가 구조적으로 약하다.**

**대응** — 완화 질문(F2), Group B의 코드 스니펫 제시(6.5)로 진입 장벽을 낮춘다. Phase 0(18장)에서 검증하고, 실패 시 커리큘럼 배치형으로 포지셔닝 전환(4장).

### Risk 2. 맞는 설명을 틀렸다고 판정한다 (FP)

**초급 학습자는 평가 이유를 검증할 능력이 없다.** 이유를 보여주는 것만으로는 대응이 되지 않으며, 오히려 잘못된 힌트가 없던 오개념을 심는다.

**대응** — 원칙 7(`확인필요` + 확인 질문), 원칙 8(이의 제기), FP 목표 0건(15.1), FP 전용 셀 3건/개념(16.1), 수용 기준 A2.

**v3에서 발견된 실제 사례:** v2의 `프로토콜 — 규칙·약속` 필수 요소는 원문에 근거가 없어, 원문을 성실히 읽은 학습자에게 구조적으로 FP를 발생시켰다(6.3). **FP는 모델이 아니라 루브릭에서 먼저 생긴다.**

### Risk 3. 품질이 LLM이 아니라 루브릭 저작에 좌우된다

이 제품은 실질적으로 콘텐츠 제품이다. 루브릭이 부실하면 어떤 모델을 써도 판정이 나아지지 않는다.

**대응** — 6.4 저작 프로세스(원문 인용 먼저, 앵커 실측, self-review, 불일치 시 필수 제외, 버전 관리). 판정 오류 시 프롬프트보다 루브릭을 먼저 점검.

### Risk 4. 힌트가 사실상 정답이 된다
**대응** — 3단계 분리, 정답 조기 노출 0건을 상한 제약으로 고정, 개선 계수에서 힌트 복붙 배제.

### Risk 5. 검색이 장식이 된다 (v3 신규)
루브릭 채점이 핵심이므로 검색은 빼도 제품이 돌아가는 것처럼 보인다. 그러면 임베딩·유사도·근거 칩이 전부 전시물이 된다.

**대응** — 7장에서 검색이 **루브릭으로 대체 불가능한 두 자리**(범위 판정, 교정 출처)를 명시하고, 수용 기준 D1·B2가 그 두 자리를 직접 검사한다. ③(Hint 3 문단 선택)은 MVP 규모에서 이득이 작다는 것을 문서에 그대로 적어 과장하지 않는다.

### Risk 6. LLM이 루브릭 범위를 벗어난 지식을 사용한다
**대응** — 원칙 1·9. 판정 기준은 루브릭으로 제한하고, 교정·요약·힌트 문장은 검색된 청크에 근거하며 청크 ID를 남긴다. 근거가 없으면 교정하지 않고 확인 질문으로 돌린다. 2층 `grounded`가 이것을 감시한다.

### Risk 7. 로컬 모델이 4상태 판정을 감당하지 못한다 (v3 신규)
요소 6개 × 4상태는 3B~4B 모델에게 쉬운 과제가 아니다. 일관성이 목표에 크게 미달할 수 있다.

**대응** — 15.2. 1차 측정 후 원인을 루브릭 모호성과 모델 한계로 가른다. 모델 한계면 실제 수치를 숨기지 않고 README에 공개하고 상위 모델을 권장한다.

---

## 18. 검증 계획

### 18.1 순서 (v3 변경)

v2는 Phase 0(2~3일, 참가자 6~8명, 대조군)을 구현 전에 두었다. 제출 기한 안에 불가능하고, 그 결과에 F1~F7이 걸려 있으면 **제출물의 수용 기준 자체가 미확정 상태가 된다.**

v3는 순서를 바꾼다.

| | 내용 | 시점 |
|---|---|---|
| **Phase 1** | 개념 2개 구현 + 18건 고정 세트로 품질 측정 (15.1) | **먼저. 제출 대상** |
| **Phase 0'** | Wizard of Oz 대신 **구현된 제품으로** 실사용 관찰 3~4명 | 제출 후 |

Phase 1이 먼저인 이유는 두 가지다. 첫째, 사람이 대행하는 Wizard of Oz는 **판정 일관성이라는 이 제품의 유일한 구조적 우위를 재현할 수 없다.** 사람은 세션마다 기준이 흔들리므로, 대조군(범용 챗봇)과의 차이가 씻겨 나간다. 둘째, 루브릭 저작이 이미 끝나 있으면 구현 비용이 관찰 비용보다 크지 않다.

### 18.2 Phase 0' 측정 (제출 후)

| 항목 | 정의 | 통과 기준 |
|---|---|---|
| 첫 설명 시도율 | 첫 질문에 2문장 이상으로 답한 비율 | 참가자 4명 중 3명 |
| 개념 완주율 | 달성 + 소진 종료 (3.1) | 4명 중 3명 |
| 재설명 응답률 | 힌트를 받은 뒤 다시 설명을 시도 | 4명 중 3명 |
| 이해율 | 달성 종료만 (3.1) | 참고 지표 |

**중단 기준** — 첫 설명 시도율이 절반 미만이면 자기 설명 유도 방식 자체를 재설계한다(설명형 대신 선택형으로 진입한 뒤 설명으로 전환). 대체재(범용 챗봇 + 프롬프트)와 완주율 차이가 없으면 별도 제품이 아니라 프롬프트 배포로 방향을 바꾼다.

**부산물** — 수집된 실제 답변으로 16.1 세트의 합성 항목을 교체하고, 6.4 원칙 4에 따라 예상 오개념을 실제 오개념으로 갱신한다.

---

## 19. 향후 확장

### 우선
- **선수 개념 진단** — HTTP 미도달 시 클라이언트/서버 상태를 먼저 확인
- **미도달 요소 누적 추적** — 반복적으로 막히는 요소를 개념 간 비교로 파악
- **개념 확대** — 레이아웃, 이벤트. Group B 루브릭 저작 방법이 검증된 후

### 조건부
- **출처 확장(공식 문서·교과서)** — Risk 3에 따라 저작 비용이 개념 수에 선형 증가한다. 자동 요소 추출 + 사람 검수 하이브리드 파이프라인이 먼저 성립해야 한다. 그 파이프라인 없이 출처를 늘리는 계획은 실현 가능성이 낮다.
- **적응형 학습 경로** — 이해 상태 데이터가 충분히 축적된 후

---

## 20. 실험 대상 (바꿀 수 있는 것)

약속이 아니라 실험 대상이다. 한 번에 하나씩만 바꾸고 16.1 고정 세트로 비교한다.

- 범위 판정 임계값 (초기 0.55)
- 검색 개수 `topK` (초기 5)
- 루브릭의 필수/선택 구분 — 특히 RB-A5
- 힌트 3단계의 문구 강도
- 확인 질문의 발동 조건 (`확인필요` 판정 기준)
- 답변 모델 (`qwen3:4b` / 상위 모델)

---

## 21. 최종 제품 정의

Mindi은 웹 개발 지식을 대신 설명해주는 AI가 아니라, 학습자가 배운 내용을 스스로 설명하게 만들고 **사전에 저작된 루브릭**을 기준으로 어떤 요소가 비어 있고 무엇이 잘못되어 있는지 되돌려주는 학습 도구다.

> **"읽었는지를 확인하는 것이 아니라, 설명할 수 있는지를 확인한다."**

이 문장이 성립하려면 두 전제가 필요하다.

1. **학습자가 설명을 시도한다** — Phase 0'가 검증한다.
2. **판정이 믿을 만하다** — 루브릭이 원문에 근거하고(6.3), 진단이 근거를 남기며(12장), 맞는 설명을 틀렸다고 하지 않는다(15.1 FP 0건).

두 번째 전제가 이 문서가 실제로 다루는 것이다.
